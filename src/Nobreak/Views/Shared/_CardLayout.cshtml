@{ 
    Layout = "_Layout";
}

<div class="container">
    <div class="card">
        <div class="card-body">
            @RenderBody()
        </div>
    </div>
</div>

@section Scripts {
    @RenderSection("Scripts", false)
}